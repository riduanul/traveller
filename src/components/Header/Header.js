import React from 'react';
import logo from '../../resorces/Logo.png';

const Header = () => {
    return (
        <div>
            <img src={log} alt=""/>
            <input type="text"/>
        </div>
    );
};

export default Header;